1. 根据addiw a0,a0,3知函数f和g参数放在寄存器a0中；根据li a2,13知寄存器a2保存了main对printf调用时的13。参考其它资料可以知道，a0-a7存放参数。
在C代码中，main调用f，f调用g。而在生成的汇编中，main函数进行了内联优化处理，对f是直接计算出了12，对于g则是内联到了地址14到1a之间。
printf被放置在了0x630的位置
ra中保存的是0x30。其意义为与十进制下1536相加，得到16进制的0x630，也就是printf的地址。
57616=0xE110，0x00646c72小端存储为72-6c-64-00，对照ASCII码表72:r 6c:l 64:d 00:充当字符串结尾标识，因此输出为：HE110 World。若为大端存储，i应改为0x726c6400，不需改变57616。
打印寄存器a2中的值。需要两个参数，却只传入了一个，因此y=后面打印的结果取决于之前a2中保存的数据。
